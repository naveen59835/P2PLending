eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_SERVER_IP:localhost}:${EUREKA_SERVER_PORT:8761}/eureka
    register-with-eureka: true
    fetch-registry: true

server:
  port: 9002

spring:
  application:
    name: "api-gateway"
